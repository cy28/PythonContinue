# 常用字符

"""
\d: 匹配数字，0-9之间的任意数字
\D: 匹配非数字字符
[^0-9]: 匹配不是0-9的字符，相当于 \D
\w: 匹配数字 字母 下划线，相当于[0-9a-zA-Z_]  且只匹配一次
\W: 匹配非数字 非字母 非下划线，相当于[^0-9a-zA-Z_]  且只匹配一次
\s: 匹配任意空白字符，包括空格、制表符\t、换行符\n、回车\r、换页\f。
\S: 匹配任意非空白字符
[]: 表示匹配括号类的一位
-: 表示一个区间
.: 表示除换行符之外的任意字符
*: 匹配前一个字符的零次或多次重复。>=0
+: 匹配前一个字符的一次或多次重复。>=1
?: 匹配前一个字符的零次或一次重复。1或0
{n}: 匹配正好n次。
{n,}: 匹配至少n次。
{n,m}: 匹配至少n次，但不超过m次。
"""


# 总介绍
1. 普通字符：除了具有特殊含义的字符外，所有普通字符（如字母、数字、标点符号等）在正则表达式中都表示它们本身。

    a. 字母和数字：
    所有字母（a-z和A-Z）和数字（0-9）在正则表达式中表示它们本身。

    b. 标点符号：正则表达式中的常见标点符号包括：
    .：匹配任意字符（除了换行符）。
    ,：逗号。
    ;：分号。
    :：冒号。
    !：感叹号。
    ?：问号。
    -：减号。
    _：下划线。
    "：双引号。
    '：单引号。
    ()：圆括号。
    []：方括号。
    {}：花括号。
    <>：尖括号。
    @：at符号。
    #：井号。
    $：美元符号。
    %：百分号。
    &：和符号。
    *：星号。
    +：加号。
    =：等号。
    ~：波浪号。
    |：竖线。

    c. 空白字符：正则表达式中的空白字符包括：
    空格符。
    制表符（\t）。
    换行符（\n）。
    回车符（\r）。
    垂直制表符（\v）。
    换页符（\f）。


2. 元字符：具有特殊含义的字符被称为元字符。以下是一些常用的元字符：

    .: 匹配除换行符外的任意字符。
    ^: 匹配字符串的开头。
    $: 匹配字符串的结尾。
    *: 匹配前一个字符的零次或多次重复。
    +: 匹配前一个字符的一次或多次重复。
    ?: 匹配前一个字符的零次或一次重复。
    \: 转义字符，用于匹配特殊字符本身，如\.匹配点号。
    []: 字符类，匹配方括号中任意一个字符。例如，[abc]匹配'a'、'b'或'c'。
    |: 或操作符，匹配两个模式中的任意一个。例如，A|B匹配'A'或'B'。
    (): 分组，用于对一部分正则表达式进行分组。


3. 重复限定符：用于限制前一个字符或子表达式的重复次数。

    *: 匹配零次或多次。
    +: 匹配一次或多次。
    ?: 匹配零次或一次。
    {n}: 匹配正好n次。
    {n,}: 匹配至少n次。
    {n,m}: 匹配至少n次，但不超过m次。


4. 字符类：用于指定一组可选字符。

    [...]: 匹配方括号内的任意一个字符。
    [^...]: 匹配除了方括号内的字符之外的任意字符。




